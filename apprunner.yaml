version: 1.0
runtime: python38
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: python -m streamlit run streamlit.py --server.port=8080 --server.address=0.0.0.0 --server.headless=true
  network:
    port: 8080